{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "debug",
      "type": "shell",
      "command": "mvn",
      "args": [
        "spring-boot:run",
        "-Dspring-boot.run.jvmArguments=-agentlib:jdwp=transport=dt_socket,server=y,suspend=y,address=5005"
      ],
      "group": "build",
      "cwd": "${workspaceFolder}/dev_env/sprint-boot-sample",
      "options": {
        "cwd": "${workspaceFolder}/dev_env/sprint-boot-sample"
      }
    }
  ]
}
